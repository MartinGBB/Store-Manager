// const newError = (ERROR_CODE) => {
//   const { status, message, code } = ERROR_CODE;
//   const err = new Error(message);
//   err.status = status;
//   err.code = code;
// };

// module.exports = newError;
